# CONTRIBUTING — 如何参与

欢迎任何形式的参与。这个仓库的目标是成为一条"普通人可跟随"的 AI Engineer 学习路线，
你的踩坑记录、勘误、练习解法，对后来者都是高价值内容。

## 参与方式

| 方式 | 说明 |
|------|------|
| 勘误 | 课程/代码/文档中的错误，直接提 Issue 或 PR |
| 补充练习 | 往 `exercises/` 对应 Phase 目录添加题目 + 参考解法 |
| 踩坑记录 | 往 `mistakes/` 按固定格式新增一条真实踩坑 |
| 文章素材 | 往 `content/articles/` 投稿学习笔记（要求见下） |
| 项目改进 | `projects/` 下各项目的功能增强 |

## 内容要求

1. **面向初学者**：假设读者有 Java/前端基础但不懂 Python 和 AI。
2. **可运行**：所有代码在写明的环境版本下验证过；给出运行命令和预期输出。
3. **Java 对照**：讲新概念时尽量给 Java 类比，但必须指明机制差异。
4. **不堆术语**：少 AI 套话，多真实问题和踩坑过程。
5. **原创**：不复制其他仓库/教程的内容；引用需注明来源。

## 提交规范

```text
feat:     新功能/新课程/新项目
fix:      修复
docs:     文档
refactor: 重构
test:     测试
chore:    杂项
experiment: 实验
```

## 流程

1. Fork / 建分支
2. 修改并自测（相关项目 `python -m unittest discover -s tests`）
3. 提 PR，说明改了什么、为什么

## 安全红线

严禁提交：API Key、Token、密码、SSH 私钥、`.env` 文件。
`.env` 只允许通过 `.env.example` 模板提供占位符。
